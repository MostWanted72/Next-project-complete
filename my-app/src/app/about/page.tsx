import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "About Page",
  description: "This is about page for next.js learning",
};

export default function About() {
  return <div>About Page</div>;
}
